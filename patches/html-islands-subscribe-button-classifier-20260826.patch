diff --git a/project-sources/html_islands.py.txt b/project-sources/html_islands.py.txt
--- a/project-sources/html_islands.py.txt
+++ b/project-sources/html_islands.py.txt
@@
-    if 'buttoncreatebutton' in low or '%%share_url%%' in low:
-        return "share", "rich_html"
+    # Checkout-backed ButtonCreateButton is a Subscribe control. Check it
+    # before the generic ButtonCreateButton/Share branch so inventory identity
+    # remains correct even when Substack uses the same component wrapper.
     if 'subscribewidgettodom' in low or 'subscription-widget-wrap-editor' in low or '%%checkout_url%%' in low:
         return "subscribe", "rich_html_candidate"
+    if 'buttoncreatebutton' in low or '%%share_url%%' in low:
+        return "share", "rich_html"
