# CHAT-SHAKING-C-AMBIGUITY-RECOVERY-001 — recover C before any further paid action

Task: `somatic-r15-clean-continuation-20260830`

Status: **FROZEN CHAT-AUTHORED RECOVERY PACKET / CODEX MECHANICAL EXECUTION ONLY**

## Decision basis

The Shaking guidance/social family currently contains:

- H0: completed Human `1.0` control;
- A: completed Human `0.8051861524581909` / AI `0.1948138326406479`;
- B: completed Human `1.0` / AI `0.0`;
- C: one authenticated local-GUI click occurred after a durable reservation, but `wait_report` timed out and the first read-only History recovery did not find an exact stored record;
- D/E: never submitted.

C is therefore `EXACT_GUI_ACTION_AMBIGUOUS_HISTORY_NOT_FOUND`. The click may have produced a paid action even though no exact History record was found. The existing reservation blocks another C submission. `--force` is not authorized.

The next operation is recovery-first. No new detector action may occur until C has been exact-bound to a complete Pangram 4.0 result. If C is recovered, D and E may then continue serially under the exact original family contract. If C remains unresolved after the bounded recovery sequence below, D/E remain untouched and the worker stops.

## Worker role

Mechanical executor only.

Do not:

- generate, revise, select, apply, diagnose, or interpret prose;
- infer whether C probably succeeded or failed;
- repeat C;
- use `--force`;
- submit through the API;
- open another detector family;
- run a whole-document detector action;
- modify any article candidate or registered `master.html`;
- clear, reset, replace, or delete the persistent Pangram browser profile;
- close a possibly relevant restored Pangram tab before capturing its state.

## Exact article/input authority

Article repository: `u-dont-existDOTcom/joel-articles`

Article branch: `task/somatic-r15-clean-continuation-20260830`

Packet source ancestor: `8b46107c5716a9cae7a2526502b2c4044ff6eba4`

Current non-authoritative production candidate:
`articles/somatic-therapies/experiments/R15-EFT-REPAIR-CANDIDATE-20260831.md`

Expected candidate Git blob:
`6f9251f51d79a6b322b8c6f6cae95a9a5d80f760`

Expected candidate UTF-8 SHA-256:
`5a6226ca0056610b4492de7713a43bb152dde1079d81b5c05896c70fcf138679`

Frozen input source commit:
`6be6e6513326bb437faceeed2579a87c41ff1d83`

Frozen input directory:
`tasks/somatic-r15-clean-continuation-20260830/surface-experiment-008/`

### C — current Shaking anchor alone

Path: `C-current-anchor.txt`

Git blob: `2993757f24707297f1c8dd7b3fbf6c4e017e9e0b`

UTF-8 SHA-256: `b36e1e46c06d764a080d407dce5412defe76ccb9202deb1a8a14e265acf40370`

Identity: `293` whitespace words / `1631` Unicode characters / `1641` UTF-8 bytes / no terminal newline.

Existing C reservation time:
`2026-08-31T04:04:17.182276Z`

Existing C failure capture time:
`2026-08-31T04:07:24.622478Z`

Existing C classification:
`EXACT_GUI_ACTION_AMBIGUOUS_HISTORY_NOT_FOUND`

### D — current anchor + current guidance/social tail

Path: `D-current-anchor-current-tail.txt`

Git blob: `261e9410bbc89e335a31f10e0e5e7e844788382e`

UTF-8 SHA-256: `b27446d695044a6749a2780f7629599177160d4a619758784b915b3cdc013900`

Identity: `328` whitespace words / `1838` Unicode characters / `1848` UTF-8 bytes / no terminal newline.

### E — current anchor + direct guidance/social tail

Path: `E-current-anchor-direct-tail.txt`

Git blob: `ad9e793d8073f859b555d5e0262c651303e6972b`

UTF-8 SHA-256: `75fb8426a498dcedb99bb7ed84d9f5a6e1653a29b6e2b7d5ff8f61fe5fb8563f`

Identity: `333` whitespace words / `1857` Unicode characters / `1867` UTF-8 bytes / no terminal newline.

Direct-tail UTF-8 SHA-256:
`0dd08ace8f74a3a1aac3f6bf6010808940ffcdd4b7da9e1a7a650b74a17d1e38`

Enforce every source-binding, identity, and preservation assertion already frozen in:
`tasks/somatic-r15-clean-continuation-20260830/CHAT-SURFACE-EXPERIMENT-008.md`

## Exact detector state

Detector repository: `u-dont-existDOTcom/pangram-humanization-lab`

Detector branch: `task/somatic-r15-exact-recovery-20260830`

Expected starting head:
`8bc3c8ba5341158ab82b1942500932665779d51c`

Existing partial result packet:
`state/experiments/somatic-r15-shaking-human-anchor-guidance-social-tail-20260831/RESULT-PACKET-GUI-CDE.json`

Existing packet SHA-256:
`4b55af7315022ca218b538c190af642b1c50b3200d98df0b762650fd8004df74`

Stable family:
`somatic-r15-shaking-human-anchor-guidance-social-tail-20260831`

Do not reset, force-push, delete, replace, or overwrite prior evidence.

## Phase 1 — preserve and inspect the exact failed-run state

Before launching or navigating anything:

1. Locate the existing local C run/reservation/failure directory and verify its exact input hash.
2. Preserve/hash every non-secret run artifact already present, including reservation, failure, screenshot/PDF/HTML, trace/HAR/network metadata, runner stdout/stderr, task checkpoint, and result placeholders where they exist.
3. Do not commit cookies, browser storage values, authorization headers, private History URLs/UUIDs, or raw unrelated History records.
4. Detect whether the dedicated Pangram browser process and its original tab still exist. If they do, capture the current URL class, page title, visible status/error text, button state, exact-input presence, and a privacy-safe screenshot before any navigation or close action.
5. If the browser process is gone, relaunch only through the same dedicated persistent profile. Do not create a fresh profile and do not clear session state.

## Phase 2 — exhaustive read-only C recovery

C recovery has **no detector-submission path**.

1. Run authenticated read-only status/auth verification.
2. Run `pangram-local recover` for exact C with the expected SHA gate.
3. Inspect the restored/current detector tab read-only for:
   - an exact C result;
   - a Pangram error or insufficient-credit response;
   - an in-progress/pending state;
   - a report/history route;
   - the exact C text still present in the detector input.
4. Enumerate Pangram History to exhaustion, not to a fixed retained-candidate count:
   - exhaust UI scrolling/pagination;
   - exhaust authenticated `history-list` pages/records available through the app;
   - inspect every record created around and after the C reservation time;
   - exact-bind only under the documented bounded transport normalizations.
5. Inspect already-persisted Playwright/network/trace evidence from the failed run for any provider response, task/report identity, POST outcome, client-side rejection, or report navigation. This is read-only and must not expose secrets.
6. Inspect the dedicated profile's current application state, service-worker/cache/IndexedDB/localStorage metadata only where the existing runner already supports privacy-safe read-only inspection. Do not modify browser storage.
7. If a report/history route is recovered, use direct read-only lookup/localization with the exact C identity gate. Never persist the private route itself.

## Phase 3 — bounded delayed recovery

History propagation may lag the first scan. Perform a bounded recovery sequence:

- complete one exhaustive read-only scan immediately;
- complete a second exhaustive read-only scan at least `10` minutes later;
- ensure the final scan occurs no earlier than `30` minutes after `2026-08-31T04:07:24.622478Z`.

If that 30-minute point has already passed when execution begins, perform the immediate exhaustive scan and one additional exhaustive scan at least 10 minutes later.

Do not click C during or after these scans.

## C recovery classifications

Persist exactly one final C classification:

### `EXACT_GUI_RESULT_EXISTS_RECOVERED_AFTER_AMBIGUITY`

Use only if an exact stored C record is recovered with:

- exact/bounded stored-text identity;
- Pangram version `4.0`;
- terminal `STAGE_SUCCESS`;
- complete `response.overall` Human/AI/AI-assisted fractions;
- durable History/result binding.

### `EXACT_GUI_ACTION_AMBIGUOUS_RECOVERY_EXHAUSTED`

Use if no exact C result is found after all bounded scans and state inspection.

### `EXACT_GUI_ACTION_AMBIGUOUS_PROVIDER_REJECTION_OBSERVED`

Use if a provider/client rejection is observed after the click but the available evidence does not prove that no paid detector action occurred. This remains ambiguous and does not authorize repeat.

Do not classify C as never submitted merely because History remains empty. Do not automatically clear the reservation.

## Conditional continuation to D and E

Only if C reaches `EXACT_GUI_RESULT_EXISTS_RECOVERED_AFTER_AMBIGUITY` with Pangram `4.0` / `STAGE_SUCCESS` may the worker continue automatically to D and E.

For D, then E, serially:

1. verify exact bytes/counts/preservation;
2. check exact local cache, reservations, checkpoints, and authenticated History;
3. persist `EXACT_GUI_RESULT_EXISTS`, `EXACT_GUI_ACTION_AMBIGUOUS`, or `EXACT_GUI_NEVER_SUBMITTED` before any click;
4. recover an existing result if present;
5. only for `EXACT_GUI_NEVER_SUBMITTED`, write/push a durable pre-click reservation and submit exactly once through the authenticated local GUI;
6. require exact History binding, Pangram `4.0`, and `STAGE_SUCCESS`;
7. persist the complete result before proceeding;
8. if D becomes ambiguous, stop before E;
9. if E becomes ambiguous, stop with E unresolved;
10. never use `--force`.

Maximum new detector actions in this packet:

- C GUI clicks: `0`;
- D GUI clicks: `0..1`, conditional on recovered C;
- E GUI clicks: `0..1`, conditional on recovered C and completed D;
- API actions: `0`;
- whole-document GUI actions: `0`.

## Durable output

Write under:
`state/experiments/somatic-r15-shaking-human-anchor-guidance-social-tail-20260831/`

Required packet:
`RESULT-PACKET-GUI-CDE-RECOVERY-002.json`

It must include:

- exact detector branch heads before/after;
- C reservation/failure identities and privacy-safe artifact hashes;
- count and timing of each read-only recovery scan;
- History candidate/page/record counts for each scan;
- whether the original/restored tab yielded result, error, pending, or no adjudicative state;
- whether any provider/task/report identity was recovered;
- exact final C classification;
- exact C result if recovered;
- exact D/E classifications/results if conditionally executed;
- complete H0/A/B/C/D/E target table where available;
- requested target/telemetry deltas where mathematically comparable;
- `new_c_clicks: 0`;
- `new_d_clicks: 0..1`;
- `new_e_clicks: 0..1`;
- `new_api_calls: 0`;
- `whole_document_calls: 0`;
- `article_mutations: 0`;
- `registered_master_mutations: 0`;
- `force_overrides: 0`;
- exact family state.

Do not persist private History URLs, UUIDs, credentials, cookies, storage contents, or unrelated record text.

## CI disposition

Carry forward without unrelated edits:

- `FULL_HISTORY_FIX: PASS`;
- `REMAINING_VALIDATOR_FINDINGS: PRE_EXISTING_UNRELATED_MERGE_DEBT`;
- `HUMANIZATION_EXECUTION_BLOCKED: NO` only if C is recovered and conditional D/E execution is safe;
- `HUMANIZATION_EXECUTION_BLOCKED: AMBIGUOUS_PAID_ACTION_RECOVERY_REQUIRED` if C remains unresolved;
- `MERGE_BLOCKED_UNTIL_RECONCILED: YES`.

## Stop boundary

If C remains ambiguous after bounded recovery:

- do not click D or E;
- do not clear the C reservation;
- persist the recovery packet;
- return the exact unresolved classification;
- stop.

If C is recovered and D/E complete:

- set family state `CLOSED_6_OF_6`;
- do not apply D or E to the article;
- do not generate another variant;
- do not run the final whole-document GUI measurement;
- persist and return the exact packet;
- stop.

Chat owns all interpretation and the next production decision.
