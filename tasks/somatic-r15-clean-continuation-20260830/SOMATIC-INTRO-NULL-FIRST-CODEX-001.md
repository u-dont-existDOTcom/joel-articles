# SOMATIC INTRODUCTION — NULL-FIRST CODEX 001

Task: `somatic-r15-clean-continuation-20260830`

Role: **Codex mechanical executor only**

This packet authorizes one writer transport and capture. It does not authorize prose generation by Codex, substantive editing, validation judgment, article mutation, or detector use.

## Canonical identity

Repository:

`u-dont-existDOTcom/joel-articles`

Branch:

`task/somatic-r15-clean-continuation-20260830`

Source:

`articles/somatic-therapies/experiments/R15-EFT-SHAKING-SOCIAL-REPAIR-CANDIDATE-20260831.md`

Required source Git blob:

`22126723d8c585ca8bde54f00c4ade6c925f354e`

Controlling decision:

`tasks/somatic-r15-clean-continuation-20260830/SUPERVISOR-DECISION-SOMATIC-HUMANIZATION-FRESH-REASONING-003.md`

Exact writer packet:

`tasks/somatic-r15-clean-continuation-20260830/SOMATIC-INTRO-NULL-FIRST-WRITER-001.md`

## Hard role boundary

Codex may:

- verify repository, branch, source blob, packet presence, and exact target extraction;
- open one genuinely fresh reasoning/writing chat;
- transmit the exact writer packet and source access instruction;
- capture the exact response;
- materialize exact bytes;
- compute hashes and counts;
- run deterministic edit-algebra checks;
- commit the raw response and mechanical receipt;
- return evidence.

Codex may not:

- write, rewrite, complete, repair, normalize, select, or judge article prose;
- explain the task to the writer in new substantive words;
- add examples or a semantic checklist;
- expose the semantic-custodian packet to the writer;
- ask the writer for a second version;
- decide whether the candidate reads as Human or AI;
- perform semantic adjudication;
- apply the candidate to any article file;
- open validators without a later exact Chat directive;
- run Pangram or any detector;
- modify registered `master.html`.

## Preflight

1. Read repository authority fresh according to `SKILL.md` and `CANONICAL-REPO-MAP.md`.
2. Resolve actual branch head and record it.
3. Require exact source blob `22126723d8c585ca8bde54f00c4ade6c925f354e`.
4. Require all four packet paths named in the controlling decision to exist.
5. Require no existing completed writer response under the output family below. Reuse exact completed evidence; never create a duplicate attempt.
6. Fail closed on any mismatch.

## Exact transport

Open one new reasoning/writing context with no prior Somatic conversation history.

Transmit only this instruction:

> GitHub is canonical. On branch `task/somatic-r15-clean-continuation-20260830`, execute `tasks/somatic-r15-clean-continuation-20260830/SOMATIC-INTRO-NULL-FIRST-WRITER-001.md` exactly. Follow its authority read and information firewall. Return only the packet's required output.

Do not append a summary, semantic notes, detector history, prior failures, owner exemplars, or advice.

Capture:

- exact raw response bytes;
- model/mode as displayed;
- chat URL or stable local transport identity, without credentials;
- start and completion timestamps;
- actual repository branch/head observed by the writer if returned;
- whether the response is `NO_VALID_SOURCE_BOUND_CANDIDATE`.

## Durable output

Create only after a complete response:

`tasks/somatic-r15-clean-continuation-20260830/null-first-intro-001/WRITER-RAW.md`

`tasks/somatic-r15-clean-continuation-20260830/null-first-intro-001/MECHANICAL-RECEIPT.json`

The raw file must contain the exact response without normalization.

The receipt must include:

- schema version;
- task/decision/writer-packet identities;
- source path, source blob, and source SHA-256;
- branch head before transport and commit head after capture;
- model/mode and non-secret transport identity;
- timestamps;
- raw response Git blob and SHA-256;
- UTF-8 byte, character, line, and whitespace-word counts;
- response type: `CANDIDATE` or `NO_VALID_SOURCE_BOUND_CANDIDATE`;
- exact mechanical checks below;
- article mutations: `0`;
- registered-master mutations: `0`;
- detector actions: `0`;
- validator actions: `0`.

## Mechanical checks for a candidate

These checks are syntactic and provenance-mechanical only. They are not semantic or editorial judgment.

Require:

1. exactly one `### CANDIDATE`, one `### EDIT_SCRIPT`, and one `### LEXICAL_DELTA` block;
2. candidate begins with exact `# Introduction`;
3. candidate does not include the stop heading or later article text;
4. exact inner-child URL occurs once and is unchanged;
5. declared added lexical tokens = `0`;
6. declared substituted lexical tokens = `0`;
7. every candidate lexical token, after case normalization and Markdown-token handling, is available in the source boundary with no greater multiplicity than the source, except the required output labels outside the candidate;
8. every `MOVE` or `DELETE` source span in the edit script occurs byte-identically in the source target;
9. every moved span occurs byte-identically in the candidate;
10. no candidate sentence contains a novel lexical sequence unsupported by retained or moved source spans;
11. punctuation/capitalization changes are enumerated;
12. the response contains no second candidate, self-critique, verdict, Pangram reference, or prose recommendation.

If a deterministic check fails, classify:

`MECHANICAL_EDIT_ALGEBRA: FAIL`

Persist the exact failure. Do not repair, retry, or ask the writer to revise.

If every deterministic check passes, classify:

`MECHANICAL_EDIT_ALGEBRA: PASS`

Do not infer semantic preservation or discourse-shape success.

## Commit and return

Commit only the raw response and mechanical receipt to the active branch with a narrow commit message.

Return only:

- actual preflight branch/head;
- source identity result;
- writer response type;
- raw response and receipt paths;
- hashes/counts;
- mechanical check results;
- capture commit SHA;
- confirmation of zero article/master/detector/validator actions.

Stop. Chat owns every next decision.
