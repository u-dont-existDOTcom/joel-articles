# Romance semantic fidelity — r7 addendum — 2026-08-21

Status: **blocking correction to the earlier audit until r8 restoration**. Canonical `main` remains unchanged.

The first semantic-r7 materialization restored the seven unsuperseded loss units then known from the registered→r6 audit:

- 2 in `The Queen of Orgasms`;
- 4 in `Not A Performance`;
- 1 in `After leaving`.

r7 master SHA-256: `00698c96b42b5f4a24ea1078c6fbcb8ff068a403720d3bc38e091e1656db8c3c`.

The exact registered→r7 section manifest confirmed the same 44-heading sequence and the same finite set of 16 changed sections. A subsequent prose-level diff audit of the restored `Not A Performance` section found two additional registered argumentative functions that the earlier r6 ledger had failed to enumerate:

1. `Then I have to defend the identity every time I hesitate, cry, need help, or get something wrong.`
   - Function: identity-performance creates a causal need to defend masculinity against ordinary vulnerability.
   - r7 contains the vulnerability examples but not this causal identity-defense claim.

2. `Surrender means so much more when she could take control but prefers not to at that moment.`
   - Function: chosen surrender has greater meaning precisely because the woman retains agency/capacity to take control.
   - r7 retains the later driving/choice example, but not this explicit governing claim.

These are not owner-superseded. Treating them as merely implied would silently weaken the registered argument, so they remain blocking losses.

`SEMANTIC-TRACEABILITY-R6.json` has been corrected from 7 to **9** unsuperseded lost units total. r7 restored the original seven, leaving these **2** still unresolved.

Decision: build a deterministic r8 from exact r7 that restores only these two sentences, preserves Part 1 byte-for-byte, preserves all direct owner / owner-final spans, and then regenerate the registered→candidate traceability scope. No detector call is justified before that semantic gate passes.
