# Romance r22 surviving proposals — function-coverage re-audit — 2026-08-24

Status: **assistant editorial re-audit after owner process correction. This does not override Joel's owner wording/dispositions and does not change article bytes.**

Method: apply the newly promoted function-first feature-recovery gate to every surviving proposal: inspect the complete current natural section and relevant adjacent material for the same function before recommending any transplant.

## 1. Talk about making love before you do it — KEEP

Proposed function: make a prospective plan for future libido divergence before resentment/hurt begins.

Current r22 already says people change and communication should remain open beyond the initial interview, but it does **not** explicitly ask the couple to discuss in advance what they will do if libidos later diverge.

Coverage: `partial`.

Recommendation: keep Joel's proposed addition:

`If our libidos later diverge, it's better to talk about what we'd do before either person is already hurt.`

## 2. Affection and the simmer — KEEP, with one distinction

Current r22 already has `I want to know what changed`, so generic curiosity about a changed sex life is not a missing function by itself.

Joel's revision adds several genuinely distinct functions/details:
- the `taking each other for granted` diagnosis;
- the five-years-ago contrast;
- the anti-homework joke;
- resisting passive drift into a `new normal`;
- `one of us doesn't feel wanted` as a concrete relational variable.

Coverage: mixed; generic curiosity is `equivalent`, the other additions are `absent`.

Recommendation: keep the owner-edited revision because the new-normal / wanted / anti-homework functions add real content beyond the existing `what changed?` sentence.

## 3. If slow isn't realistic for you — RECOMMEND DROP

Proposed recovery: add `while we figure out what this actually is` to the sentence about community slowing the couple down.

But the same section already opens:

`I pretty much always intend to wait long enough to have this conversation and figure out who I'm dealing with.`

It later returns to the same discernment purpose: wanting someone can happen much faster than knowing whether to raise children, live in community, or build a life together.

Coverage: `equivalent/stronger` already in current r22.

Recommendation: do **not** add the older purpose clause. It would restate the section's already-established reason for slowing down.

## 4. Can making love be a spiritual practice? — KEEP

Two proposed functions:
1. make testimonial provenance explicit: these are stories from students **who say** the effects spill into other areas of life;
2. replace `jade-egg practice is basically the solo version` with Joel's more specific training relation.

Neither function is duplicated elsewhere in the current section.

Coverage: `absent`.

Recommendation: keep, with the mechanical grammar correction `preliminary training for the cervical O` if that is Joel's intended wording.

## 5. Muses & Directors — RECOMMEND DROP

Proposed addition:

`I also feel useful as the one who translates the poetry into function, when appropriate.`

The current section already says:
- `I help give it direction and make it operational.`
- `Where I like the polarity is when she sees the Big Picture, and I help fill in the details to make our plan workable.`

Those are the same function, stated twice already and with more concrete meaning.

Coverage: `stronger/more concrete` already in current r22.

Recommendation: do **not** add the proposed sentence. This was another phrase-level recovery mistake.

## 6. Two Pillars Don't Hold The Roof Up — KEEP ONLY THE ONE-SENTENCE REMAINDER

The old three-sentence block carried three functions. Current r22 already carries:
- the `wrong person to help` function through the B. projection paragraph;
- the two-sided reality-check function through B. and H. (`Baseless accusations only work with friends who only know one side`; `The accusations couldn't have survived among people who knew us both`).

Earlier in the article, `Things get tricky fast` also says the relationship can close off exactly when both people need `a view from outside it`, so the broad outside-perspective function is already seeded.

What is still not stated explicitly is the narrower point that an outside friend may notice a **pattern neither partner has noticed**.

Coverage: two old functions `stronger/more concrete`; one function `partial/absent`.

Recommendation: keep only Joel's preferred remainder:

`But sometimes a friend who actually knows us both sees the pattern before either of us does.`

Suggested placement remains after `Community isn't magic either; if both people are falling apart, there is only so much anyone else can do.` The `But sometimes` turn now has a real contrastive job.

## 7. What are you actually choosing together? — KEEP AS OWNER WORDING CHOICE

Proposed change is not recovery of a missing function. Both versions perform the same function. The question is purely realization quality:

- r22: `I might hear sex, exclusivity...`
- older: `I can hear a whole future in those two words—sex, exclusivity...`

No competing current phrase elsewhere performs this exact image.

Coverage: function already present; wording alternative only.

Recommendation: keep as a legitimate owner wording choice if Joel prefers the stronger image. Do not describe it as restoring lost content.

## 8. Psychedelics in relationship discernment — RECOMMEND DROP THE STRESS-TEST LIST

Proposed integrated realization adds `sober, irritated, jealous, broke, bored, or trying to make a difficult decision together` after the r22 sentence about whether two people work together sober.

The function is already carried immediately afterward:

`The obvious advice is to wait before making large promises and spend time together sober, around other people, and in ordinary situations.`

It is also developed immediately before the Psychedelics section in `Imagination as a discernment tool`, which explicitly runs the relationship through conflict, money, jealousy, children, and ordinary future-life tests.

Coverage: `stronger/more developed` already in adjacent/current prose.

Recommendation: do **not** add the stress-test list. Joel's flow correction fixed the grammatical insertion problem, but the function-first audit shows the underlying addition is still redundant.

## Result

After the function-coverage audit, the assistant recommends **5** remaining changes for owner consideration:

1. prospective libido-divergence planning;
2. Affection owner rewrite/additions;
3. spiritual-practice attribution + jade-egg relation;
4. one-sentence Two Pillars missing-pattern function;
5. `I can hear a whole future...` wording choice.

The assistant now recommends dropping **3** previously surviving proposals as functionally redundant:

- slow/brakes purpose clause;
- Muses `translate the poetry into function` sentence;
- Psychedelics sober stress-test list.

These are assistant recommendations only until Joel accepts/rejects them.
